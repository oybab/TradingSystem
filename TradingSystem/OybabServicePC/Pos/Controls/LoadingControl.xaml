<UserControl x:Class="Oybab.ServicePC.Pos.Controls.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800"
             x:Name="control">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary  Source="/ServicePC;component/Pos/Styles/LoadingControlStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid Style="{StaticResource grdMessageboxParentBackgroundStyle}"  Opacity="0.7"/>
        <Border Style="{StaticResource bdMessageboxBorderStyle}" >
            <Grid Margin="30, 45">
                <WrapPanel Style="{StaticResource wpMessageboxWrapStyle}">
                    <Label Style="{StaticResource lbLoadingImageStyle}"/>
                </WrapPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
